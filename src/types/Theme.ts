export interface ThemeTypes {
  colors: {
    primary: string;
    secondary: string;
    background: string;
    border: string;
    text: string;
    buttonPrimary: string;
    buttonSecondary: string;
  };
  supportingColors: {
    primary: string;
    secondary: string;
    terciary: string;
  };
}
